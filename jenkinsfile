pipeline{
    agent any

    stages{
        stage("Docker Build"){
            steps{
                script{
                    dockerapp = docker.build("apiNodeUpload")
                }
            }

        }
    }
}