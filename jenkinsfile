pipeline{
    agent any

    stages{
        stage("Docker Build"){
            steps{
                script{
                    dockerapp = docker.build("api-node:${env.BUILD_ID}","-f Dockerfile")
                }
            }

        }
    }
}